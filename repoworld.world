Spawn: { target = ref[manifestid, baseid, YourCustomSpawn];
	 num = 1; 
	offset = 1; 
	postion = (0,0,0); 
	scale = (0,0,0); 
	rotation = (0,0,0); 
	direction = left;
}:
Data: { YourdataKey="Your data value"; }:
|YourCustomSpawn: {
  target = placeholderid; 
	 num = 1; 
	offset = 1; 
	postion = (0,0,0); 
	scale = (0,0,0); 
	rotation = (0,0,0); 
	direction = left;
}:
